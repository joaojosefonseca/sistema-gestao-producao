[build]
  command = ""
  publish = "."

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[functions]
  directory = "netlify/functions"

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Methods = "GET, POST, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type"
```

2. **Cole** na área de texto (Ctrl + V)

---

### **Passo 6: Fazer commit**

1. Role a página para baixo
2. Vai ver uma seção **"Commit changes"**
3. No primeiro campo, escreva:
```
   Corrigir configuração netlify.toml
